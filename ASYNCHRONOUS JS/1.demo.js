console.log("ASYNC JAVASCRIPT");
console.log(
  "JAVASCRIPT is a Synchronous, Blocking and single threaded language"
);
console.log(
  "SYNCHRONOUS: executing one after one line, only one line executing at any given time"
);
console.log(
  "BLOCKING: javascript will wait at perticular line till it finishes its execution, after execution of that line code it will move to nect line of code"
);
console.log(
  "SINGLE THREADED: javascript is singles threaded language. A thread is a process to run a task. And each thread can only do one task at a time "
);
